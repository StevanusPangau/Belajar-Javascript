const company = 'Programmer Mitra';

function sum(first, second) {
  return first + second;
}

class Company {}

// contoh langsung export semua tanpa di tulis export satu" di tiap function, variable atau class
export { company, sum, Company };
